<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Mongo Scraper</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/">Home</a></li>
        <li class="active"><a href="/savedarticles">Saved Articles <span class="sr-only">(current)</span></a></li>
      </ul>
      <form action="/scrape" method="POST" class="navbar-form navbar-left">
        <button type="submit" class="btn btn-default">SCRAPE NEW ARTICLES!</button>
      </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<!--End navigation-->

<div class="container">
  <div class="row">
    <div class="col-md-12">
      PLACEHOLDER FOR JUMBOTRON AND IMAGE.
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <ul>
        {{#each articles}}
          <li>
            {{this.title}}
            {{this.link}}
            <form action="/notes/{{this.id}}" method="GET" class="navbar-form navbar-left">
              <button type="submit" class="btn btn-default">ARTICLE NOTES</button>
            </form>
            <a href="/articlenotes/{{this.id}}"><button class="btn btn-default">ARTICLE NOTES</button></a>
            <form action="/delete/{{this.id}}?_method=DELETE method="POST" class="navbar-form navbar-left">
              <button type="submit" class="btn btn-default">DELETE FROM SAVED</button>
            </form>
          </li>
        {{/each}}
      </ul>
    </div>
  </div>
  <div id="notes"></div>
</div>

<script src="https://code.jquery.com/jquery-3.1.1.min.js"></script>

<script> src="app.js"</script>